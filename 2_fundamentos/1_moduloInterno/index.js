// Exportando funçoes do arquivo "MeuMod" para o arquivo "index" e 
// realizando operaçoes basicas

const MeuModulo = require('./MeuMod')
const soma = MeuModulo.soma
const sub = MeuModulo.sub

soma(2, 3)
soma(1029547, 94947)
sub(10, 5)